name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sandbox-image:
    name: Publish sandbox image
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - name: Login to GHCR
        env:
          GH_TOKEN: ${{ github.token }}
        run: echo "$GH_TOKEN" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
      - name: Build and push sandbox image
        run: |
          set -euo pipefail
          owner="$(printf '%s' "$GITHUB_REPOSITORY_OWNER" | tr '[:upper:]' '[:lower:]')"
          ref_tag="$(printf '%s' "${GITHUB_REF_NAME:-manual}" | tr '/:@' '---')"
          publish_image() {
            name="$1"
            dockerfile="$2"
            image="ghcr.io/$owner/$name"
            docker buildx build \
              --platform linux/amd64,linux/arm64 \
              --label "org.opencontainers.image.source=https://github.com/$GITHUB_REPOSITORY" \
              --label "org.opencontainers.image.revision=$GITHUB_SHA" \
              -t "$image:$ref_tag" \
              -t "$image:latest" \
              --push \
              -f "$dockerfile" \
              .
            for arch in amd64 arm64; do
              docker buildx build \
                --platform "linux/$arch" \
                --label "org.opencontainers.image.source=https://github.com/$GITHUB_REPOSITORY" \
                --label "org.opencontainers.image.revision=$GITHUB_SHA" \
                -t "$image:$ref_tag-$arch" \
                -t "$image:latest-$arch" \
                --push \
                -f "$dockerfile" \
                .
            done
            verify_public_pull "$name" "latest-amd64"
            verify_public_pull "$name" "latest-arm64"
          }
          verify_public_pull() {
            name="$1"
            tag="$2"
            scope="repository:$owner/$name:pull"
            token="$(curl -fsSL "https://ghcr.io/token?service=ghcr.io&scope=$scope" | sed -n 's/.*"token":"\([^"]*\)".*/\1/p')"
            test -n "$token"
            curl -fsSL \
              -H "Authorization: Bearer $token" \
              -H "Accept: application/vnd.oci.image.manifest.v1+json, application/vnd.docker.distribution.manifest.v2+json" \
              "https://ghcr.io/v2/$owner/$name/manifests/$tag" \
              >/dev/null
          }
          publish_image "aithy-sandbox" "Dockerfile"
          publish_image "aithy-sandbox-lite" "Dockerfile.sandbox-lite"

  verify:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run check

  package:
    name: Package ${{ matrix.target }}
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-x64-gnu
          - target: linux-arm64-gnu
          - target: darwin-arm64
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run build
      - name: Build portable package
        env:
          TARGET: ${{ matrix.target }}
          PACKAGE_NAME: aithy-${{ github.ref_name }}-${{ matrix.target }}
        run: |
          set -euo pipefail
          bun run package:release -- --target "$TARGET" --package-name "$PACKAGE_NAME" --outdir release
          test ! -e "release/$PACKAGE_NAME/app/workers/agent-worker.js"
          test ! -e "release/$PACKAGE_NAME/app/workers/queue-service.js"
          test ! -d "release/$PACKAGE_NAME/node_modules"
      - name: Smoke packaged app
        if: matrix.target == 'linux-x64-gnu'
        env:
          PACKAGE_NAME: aithy-${{ github.ref_name }}-${{ matrix.target }}
        run: |
          set -euo pipefail
          "release/$PACKAGE_NAME/aithy" --version
          "release/$PACKAGE_NAME/aithy" --port 31987 > smoke.log 2>&1 &
          pid=$!
          trap 'kill "$pid" 2>/dev/null || true' EXIT
          for _ in $(seq 1 60); do
            if curl -fsS http://127.0.0.1:31987/ >/dev/null; then
              if curl -fsS http://127.0.0.1:31987/console > console.html \
                && grep -q 'queue-service' console.html \
                && grep -q 'sandbox-worker' console.html \
                && grep -q 'local-inference-worker' console.html; then
                kill "$pid" 2>/dev/null || true
                wait "$pid" 2>/dev/null || true
                exit 0
              fi
            fi
            if ! kill -0 "$pid" 2>/dev/null; then
              cat smoke.log
              exit 1
            fi
            sleep 1
          done
          cat smoke.log
          test ! -e console.html || cat console.html
          exit 1
      - name: Archive package
        env:
          PACKAGE_NAME: aithy-${{ github.ref_name }}-${{ matrix.target }}
        run: |
          set -euo pipefail
          mkdir -p release-artifacts
          cd release
          tar -czf "../release-artifacts/$PACKAGE_NAME.tar.gz" "$PACKAGE_NAME"
          cd ../release-artifacts
          sha256sum "$PACKAGE_NAME.tar.gz" > "$PACKAGE_NAME.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.target }}
          path: release-artifacts/*
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    needs: [package, sandbox-image]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: release-*
          path: release-artifacts
          merge-multiple: true
      - name: Create or update release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" release-artifacts/* --clobber
          else
            gh release create "$TAG" release-artifacts/* --title "$TAG" --generate-notes
          fi
